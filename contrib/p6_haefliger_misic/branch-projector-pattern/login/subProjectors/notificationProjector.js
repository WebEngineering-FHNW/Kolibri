
export { loginNotificationProjector }

/**
 * Generates a paragraph element, which will hold certain messages to notify the user on the UI
 * @returns {HTMLElement}
 */
 const loginNotificationProjector = () => {

  const pElement = document.createElement('p')  

  return pElement
}